<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeSplash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="App.Button.Login.Green.5dp.noShadow" parent="Widget.AppCompat.Button">
        <item name="android:background">
            @drawable/red_button_no_shadow_5dp_radius_selector_background
        </item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAlignment" tools:targetApi="jelly_bean_mr1">gravity</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/large_text</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="hello_text" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textColor">@color/text_dark_color</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="efficiency_indicators">
        <item name="titleTextColor">@color/white</item>
        <item name="titleTextSize">@dimen/medium_text</item>
        <item name="titleHorizontalPaddings">5dp</item>
        <item name="titleVerticalPaddings">7dp</item>
    </style>
    <style name="pie_text_view" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintLeft_toLeftOf">parent</item>
        <item name="layout_constraintRight_toRightOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">36sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
